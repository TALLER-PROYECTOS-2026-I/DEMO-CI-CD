name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  validar:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Validar HTML
        run: |
          echo "Validando proyecto..."
          ls